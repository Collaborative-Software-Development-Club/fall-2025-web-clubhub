export const scrapedClubsService = {
    uploadScrapedClubs,
};

async function uploadScrapedClubs() {
    // read the file in ../scraper/data/club-list.json
    // create a drizzle query to upload it
}
